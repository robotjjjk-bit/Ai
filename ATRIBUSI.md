# Atribusi & Lisensi

App ini menggabungkan:

1. **RikkaHub** (upstream) — `rikkahub/` submodule dari https://github.com/rikkahub/rikkahub.git (`master`, AGPL-3.0).
   Hak cipta milik kontributor RikkaHub. Lihat `rikkahub/LICENSE` untuk teks lisensi penuh.
2. **NexChat extras** di repo ini (`app/`, `web/`, `.github/workflows/`) — juga dirilis di bawah **GNU Affero General Public License v3.0 (AGPL-3.0)** agar kompatibel.

Konsekuensi AGPL-3.0: setiap distribusi / menjalankan versi modifikasi lewat jaringan wajib menyediakan source lengkap (termasuk submodule ini) kepada pengguna.
